# 안경원 정산 자동화

하남 이마트 입점 안경원의 월별 손익을 자동 계산하는 도구.
사용자는 **비개발자**다. 설명은 한국어로, 중학생이 이해할 수준으로.
결과물은 `실행.bat` 더블클릭 한 번으로 끝나야 한다. 명령어를 치게 하지 않는다.

## 사용자 상황

- 현재 이 안경원의 **직원**이고, 매장을 **인수할 예정**이라 미리 준비 중이다.
- 계좌 접근 권한이 없어 **은행 자료는 SMS 알림 스크린샷으로만** 들어온다.
  "인터넷뱅킹에서 엑셀 받으세요"는 불가능하다. 인수 후에는 가능해진다.
- 권한 확인창이 영어로 떠서 이해하기 어려워한다. 무엇을 할지 미리 알려줄 것.

## 폴더

```
01_영수증/      거래명세표 사진 (아직 대사 미구현)
02_카드내역/    신한카드 이용내역 엑셀  ← 사용자가 직접 다운로드
03_데이터/      거래처마스터.xlsx  ← 분류를 고치는 곳 (사용자가 직접 수정)
                계좌내역_YYYY-MM.xlsx  ← SMS에서 옮긴 것
                월고정비.xlsx     ← 퇴직금 충당·대출이자 등
                원장.xlsx         ← 자동 생성
                비밀번호.txt      ← 있으면 대시보드가 암호로 잠김 (지금은 없음)
04_리포트/      월별 정산리포트 (자동 생성)
05_계좌내역/    SMS 스크린샷 원본
06_매출/        "2026년 7월.xlsx" 같은 일일결산일지  ← 매출의 유일한 출처
scripts/        run.py, dashboard_template.html, 계좌내역_YYYY-MM.py
실행.bat        더블클릭 → scripts/run.py 실행 → 대시보드 자동 열림
```

## 핵심 규칙 (여기를 어기면 숫자가 틀어진다)

1. **매출은 `06_매출` 일일결산 엑셀에서만 가져온다.**
   통장 입금(카드사 정산)은 매출이 아니다 — 시차·수수료 때문에 금액이 다르고,
   매출로 세면 이중계산이 된다. 분류 `정산입금`으로 빼둔다.

2. **미수(외상)는 매출에서 제외.** 받는 날 다시 기록되므로 판 시점에 잡으면 이중계산.
   이렇게 하면 `결산` 시트의 총금액과 정확히 일치한다 (검증 완료).

3. **결제일 ≠ 귀속월.** 거래처·임대료·인건비는 월말/월초에 **전월분**을 결제한다.
   - 결제월 = 돈이 나간 달, 귀속월 = 그 물건/노동이 발생한 달
   - 순이익은 **귀속 기준**이 맞다.

4. **카드대금 출금은 비용이 아니다.** (신한카드 13,501,572 같은 것)
   카드 이용내역에 건별로 이미 잡혀 있어 또 세면 이중계산. 분류 `카드대금`.

5. **상품권**: 손님이 상품권으로 결제 → 그 상품권을 이마트에 넘겨 임대료에서 상계.
   계좌에서 나간 임대료는 이미 상품권이 빠진 금액이므로, 그 달 상품권 매출액을
   임대료에 되더해야 원래 임대료가 된다. (`sales_rows()` 가 처리)

6. **이마트는 원천에 따라 다르다.** 카드로 긁으면 장보기(개인), 계좌에서 나가면 임대료.

7. **같은 이름이 입금·출금 양쪽에 나올 수 있다.** (김상기 = 급여 출금 + 계좌이체 입금)
   거래처마스터의 열쇠는 `(원천, 방향, 표기명)` 세 개다.

8. **환불 두 종류**: `매출환불`은 매출에서 빼고, `매입환불`은 비용에서 뺀다.

## 검증 장치

- **잔액 연속성**: SMS에 잔액이 찍히므로 `계좌내역_YYYY-MM.py` 의 `verify()` 가
  빠진 거래를 자동으로 찾아낸다. 스크린샷 방식의 신뢰성은 여기서 나온다.
- **카드대금 대조**: 계좌에서 나간 카드대금 ≈ 전월 카드 사용액.
  (청구기간이 달력 월과 달라 정확히 같지는 않다)
- **결산 시트 대조**: 일일결산 합계(미수 제외) == `결산` 시트 총금액.

## 현재 상태 (2026-09-04 기준)

가진 자료: 카드 659건(4~8월) · 계좌 257건(7·8월) · 매출 5개월(4~8월)

| 월 | 매출 | 상태 |
|---|---|---|
| 2026-04 | 38,559,000 | 계좌내역 없음 |
| 2026-05 | 44,319,800 | 계좌내역 없음 |
| 2026-06 | 33,144,500 | 계좌내역 없음 |
| **2026-07** | **35,806,900** | **완전** — 순이익 3,266,804 (9.1%) |
| 2026-08 | 40,377,500 | 9월 자료 나와야 완성 |

매출 계산은 검증됐다. 4월·5월 값이 사용자 수기 장부의 총매출과 정확히 일치했고,
7·8월은 일일결산 `결산` 시트 총금액(미수 제외)과 한 원도 안 틀렸다.

### 순이익을 세 번 고쳤다 (사용자가 "안 맞는다"고 해서 찾아낸 것들)

1. **8,478,220** → 롯데·우리카드 대금 누락 발견 (월 약 120만원)
   이용내역이 없는 카드인데 "카드대금은 중복"이라며 제외해버렸다.
2. **7,282,737** → 임대료가 계좌 출금(330만)만 잡혀 있었다
3. **4,139,715** → 임대료를 월 750만원 고정으로 변경
4. **3,266,804 (9.1%, 현재)** → 카드 수수료 2.8% 반영

통장 대조로 계산한 실질 현금 창출 월 약 415만원과 근접해졌다.
남은 차이는 재고 증가와 미수 증가로 설명된다.

### 09-05 에 잡은 버그: 카드 파일 중복

`신한카드7월.xlsx` 104건이 `Shinhancard_월.xlsx`(4~7월)에 통째로 들어 있어
7월 카드 지출 12,620,733원이 두 번 잡혔다. "중복 결제 의심" 13건이 그 증상.
`read_cards()` 가 (거래일시, 가맹점명, 금액, 승인번호) 로 자동 중복 제거하도록 고쳤다.
**기간이 겹치는 파일을 넣어도 이제 안전하다. 파일을 지울 필요 없다.**

### 09-06: "대시보드에 월급이 빠졌다" — 안 빠졌고, 아직 안 들어온 것이었다

인건비는 원장에 멀쩡히 있다 (2~7월 월 770~900만원). 다만 **8월 급여는 9월에
나가서**, 9월 계좌 자료가 없으면 8월 인건비가 퇴직금 283,333원만 남는다.
그래서 "월급이 없다" 처럼 보였다.

`build_basis()` 가 이제 **아직 안 들어온 고정비를 지난 달 중앙값으로 예상**해서
`estimate` / `estTotal` / `profitEst` 로 내려준다. 대시보드는 경고 상자와
'매달 꼭 나가는 돈' 양쪽에 "아직 안 들어옴 · 예상" 으로 표시한다.
(8월: 남은 돈 2,820만 → 예상 반영 257만. 완전한 달들과 비슷한 수준이 됐다)

**진행 중인 달은 예상치를 넣지 않는다** (`inProgress`). 매출은 며칠치인데 비용만
한 달치가 붙으면 적자로 보인다. 대시보드도 **끝난 마지막 달로 열린다.**

### 09-06: 9월을 새 양식으로 옮겼다

`scripts/일지_옮기기.py` 를 만들었다.

```
python scripts/일지_옮기기.py "C:\Users\admin\Desktop\굿미소폴더\2026년 9월.xlsx" 2026 9
```

옛 양식(머리글 3·4행, 5행부터 데이터)을 읽어 새 양식으로 옮기고 **총액·건수를
원본과 대조**한다. 다르면 0이 아닌 코드로 끝난다.
9월 결과: 60줄 · 5일 · 11,105,500원 — 원본 `결산` 시트 총금액 합과 정확히 일치.

- 렌즈 이름에 `누진·다초점·기능성·프로그레` 가 있으면 **누진**, 아니면 **단초점**.
- `라이프스타일·드라이브·오피스·리딩·실내` 는 이름만으론 모른다. 단초점에 넣고
  **분홍색으로 칠한 뒤 비고에 "← 누진이면 옮겨주세요"** 를 적는다. (9월 2건)
- **줄을 합치지 않는다.** 옛 파일은 이름 없는 줄이 많아 같은 손님인지 알 수 없다.
  짐작으로 합치면 손님 수가 틀어진다.
- 원본 파일은 건드리지 않는다. `06_매출` 의 기존 파일은 `(옮기기전)` 으로 남긴다.

## 바로 다음에 할 일

1. **영수증 대사** — `01_영수증` 에 8월 사진 5장이 있으나 미구현.
   명세표 금액 ↔ 실제 결제액을 건별로 맞춰 "부당 청구"를 찾는 것이 원래 목표.
   **8월은 미완성이라 7월 명세표를 받는 게 낫다.**
2. **8월 완성** — 9월 카드내역 + 9월 계좌 SMS (10월 초에 가능)
3. **4·5·6월 완성** — 카드·매출은 있고 계좌 SMS만 없다. 달당 60~70장이라
   손으로 옮기기엔 무겁다. API 붙이는 게 맞다.
4. **우리카드 이용내역** — 지금은 "우리카드 342,900원" 한 덩어리.
   내역을 받으면 정수기·주유 등으로 갈라진다. (받으면 `STATEMENT_CARDS` 에
   "우리카드" 를 추가하고 대금은 `카드대금`(중립)으로 바꿔야 한다)

## 사용자가 정한 것 (바꾸지 말 것)

- **임대료는 월 750만원 고정.** 매달 상품권을 얼마 입금하느냐에 따라 계좌
  출금액이 달라져서 평균으로 잡기로 했다. 계좌 이마트 출금은 `임대료납부`(중립).

  계약상으로는 매출 4,000만 미만이면 기본임대료 5,323,620원(고정),
  4,000만 이상이면 매출 × 13% 다. 여기에 부가세 10% 와 관리비외 약 162만원이 붙어
  총 약 750만원이 된다. (2026-02 이마트 명세서에서 역산)

  **다만 실무상 이마트에 신고하는 매출을 4,000만 밑으로 맞추기 때문에
  항상 고정 구간이 적용된다.** 그래서 750만원 고정으로 두는 것이 맞다.
  13% 구간에 들어가게 되면 사용자가 알려주기로 했다.

  ※ 그래서 **이마트 명세서의 매출금액과 일일결산 매출은 다를 수 있다.**
    매출은 항상 `06_매출` 일일결산이 기준이다.

- **상품권은 순이익에 영향이 없다.** 임대료를 깎는 게 아니라 내는 '수단'이라
  상품권으로 내든 현금으로 내든 임대료 비용은 750만원 그대로다.
  달라지는 건 통장 잔고와 상품권 재고뿐이다.

  다만 **재고가 매달 260~310만원씩 줄고 있다.** 들어오는 상품권은 월 100~250만인데
  이마트에는 월 420만원씩 내고 있어서, 쌓아둔 재고로 메우는 중이다.
  (6월분 −2,604,137 / 7월분 −3,143,022)
  재고가 바닥나면 통장에서 나가는 임대료가 월 90만원쯤 늘어난다.
  **상품권 재고 추적 기능은 시작 재고 금액을 알면 만들 수 있다 — 아직 못 받음.**
- **인건비는 시스템 계산이 맞다.** 수기 장부의 1,000만원은 임의값이라고 했다.
- 퇴직금은 김상기 283,333원만. (장부의 588,000원은 사용자 본인 것 포함이라 별개)
- 미수는 매출에서 제외. 받는 날 매출로 잡힌다.
- **수기 장부 스크린샷은 참고만 하고 대시보드를 고치지 말 것.**
- **카드 수수료율 2.8%** (`run.py` 의 `CARD_FEE_RATE`). 매달 카드매출 × 2.8% 를
  사업경비로 자동 계산한다. 정확한 값을 알게 되면 그 숫자만 고치면 된다.
- **일일결산 양식은 "한 줄 = 한 손님".** (2026-09 부터. `일일결산_양식만들기.py`)
  품목 칸은 **안경테 / 단초점 / 누진 / 콘택트** 네 개뿐. 더 늘리지 말 것 —
  선글·기타까지 뒀더니 "복잡하다"고 해서 뺐다.

  **드롭다운에서 제품을 고르는 것이 곧 1건이다.** 개수를 따로 적지 않는다.
  건수는 `COUNTIF(범위,"?*")`, 금액은 `SUMIF(범위,"?*",합계범위)` 로 센다.

  제품 목록은 `설정` 시트 C~F열이고, `OFFSET+COUNTA` 이름범위라
  아래에 줄만 추가하면 드롭다운에 바로 나온다. 처음 목록은
  `03_데이터/사입단가.xlsx` 에서 자동으로 가져온다 (프레임→안경테, 렌즈→단초점,
  콘택트→콘택트). **누진은 사입단가표에 없어서 비어 있다 — 사용자가 채워야 한다.**
  ※ 안내 문구를 목록 칸 안에 쓰지 말 것. 드롭다운 항목으로 딸려 나온다.

  **품목별 금액은 쪼갤 수 없다.** 한 손님이 낸 돈 하나뿐이라, 품목 금액은
  '그 품목이 들어간 판매의 합계'다. 다 더하면 총매출보다 크다 — 정상이다.

  `find_columns()` 가 옛 양식(프레임/안경렌즈/콘택트)도 같이 읽는다.
  품목 건수는 새 양식(네 칸이 다 있을 때)일 때만 센다 — 옛 양식의 `콘택트` 칸은
  이름이 겹치는데 성격이 달라서 그냥 세면 엉뚱한 값이 나온다.

- **정수기 43,900원은 따로 넣지 않는다.** 우리카드로 나가는데 우리카드 대금이
  이미 통째로 사업경비로 잡혀 있어서, 또 넣으면 이중계산이 된다.

## 주의

- **`실행.bat` 는 반드시 ASCII만.** cmd 가 배치 파일을 UTF-8로 읽지 않아
  한글이 깨지고 명령이 실패한다. 한글 메시지는 `run.py` 가 출력한다.
- `비밀번호.txt` 는 `utf-8-sig` 로 읽는다. 메모장이 붙이는 BOM 때문에.
- 대시보드의 `#gate` 는 `[hidden]` 규칙을 먼저 선언해야 한다.
  `display:grid` 가 기본 숨김을 이겨버려서 잠금이 안 풀린다.
- **`DefinedName(attr_text=...)` 에 `=` 를 붙이면 안 된다.** `=OFFSET(...)` 으로 쓰면
  엑셀이 이름을 못 읽어 **드롭다운이 조용히 사라진다.** 오류도 안 뜬다.
  `showErrorMessage=True` 도 같이 줘야 경고창이 뜬다 (기본이 False).
  `showDropDown` 은 이름과 반대로 '화살표를 숨길지' 라서 `False` 가 맞다.
- **엑셀 수식은 COM 으로 실제 엑셀을 띄워 확인한다.** openpyxl 은 수식을
  문자열로만 저장해서 계산이 맞는지 알 수 없다. `IFERROR` 는 `_xlfn.IFERROR`
  라는 깨진 이름을 만드니 `IF(...)` 로 대신한다.
- **`.ps1` 에 한글을 쓰지 말 것.** PowerShell 5.1 이 BOM 없는 파일을 ANSI 로 읽어
  통째로 파싱 오류가 난다. `실행.bat` 과 같은 이유다.
